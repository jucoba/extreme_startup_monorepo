# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build extreme_startup_node_player`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t extreme_startup_node_player`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system extreme_startup_node_player && \
          adduser --system -G extreme_startup_node_player extreme_startup_node_player

COPY dist/apps/extreme_startup_node_player extreme_startup_node_player/
RUN chown -R extreme_startup_node_player:extreme_startup_node_player .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix extreme_startup_node_player --omit=dev -f install

CMD [ "node", "extreme_startup_node_player" ]
